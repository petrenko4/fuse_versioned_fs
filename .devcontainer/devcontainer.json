{
  "name": "FUSE Versioned FS",
  "build": {
    "dockerfile": "../Dockerfile"
  },
  "runArgs": [
    "--cap-add=SYS_ADMIN",
    "--device=/dev/fuse",
    "--security-opt=apparmor:unconfined"
  ],
  "remoteUser": "vscode",
  "workspaceFolder": "/workspaces/fuse_versioned_fs/"
}