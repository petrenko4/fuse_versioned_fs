{
    "tasks": [
        {
            "type": "shell",
            "label": "project-build",
            "command": "gcc -fdiagnostics-color=always -g -Wall ${workspaceFolder}/*.c `pkg-config fuse3 --cflags --libs` -o ${workspaceFolder}/myfs",
            "options": {
                "cwd": "${fileDirname}"
            },
            "problemMatcher": [
                
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "detail": "Task generated by Debugger."
        }
    ],
    "version": "2.0.0"
}